{
  "scripts": {
    "db:sync": "npx prisma db push && npx prisma db seed"
  },
  "prisma": {
    "seed": "npx tsx prisma/seed.ts"
  },
  "devDependencies": {
    "@prisma/client": "^6.12.0",
    "@types/node": "^24.1.0",
    "prisma": "^6.12.0",
    "tsx": "^4.20.3"
  }
}
